<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.english">

    <application
        android:allowBackup="true"
        android:icon="@drawable/right"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.English">
        <activity
            android:name=".story.PlayStoryActivity"
            android:exported="false" />
        <activity
            android:name=".story.StoryActivity"
            android:exported="false" />
        <activity
            android:name=".game.GameActivity"
            android:exported="false" />
        <activity
            android:name=".words.WordsActivity"
            android:exported="false" />
        <activity
            android:name=".learn_activity.LearnActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>